name: E2E (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "GitHub-hosted E2E is intentionally disabled to avoid paid/private Actions usage."
          echo "Run compat tests locally: bash compat/run-compat.sh <openclaw-version>"
